// import { createClient } from "@supabase/supabase-js";

// export const supabase = createClient(
//   "https://<project>.supabase.co",
//   "<your-anon-key>"
// );
//don't need it I am using a library for this built in
//with lot more functionality
//but we will do it manually for admin
